import { CardImageNamengPipe } from './card-image-nameng.pipe';

describe('CardImageNamengPipe', () => {
  it('create an instance', () => {
    const pipe = new CardImageNamengPipe();
    expect(pipe).toBeTruthy();
  });
});
