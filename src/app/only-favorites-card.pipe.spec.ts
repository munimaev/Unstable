import { OnlyFavoritesCardPipe } from './only-favorites-card.pipe';

describe('OnlyFavoritesCardPipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyFavoritesCardPipe();
    expect(pipe).toBeTruthy();
  });
});
