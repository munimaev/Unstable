import { CardNameFilterPipe } from './card-name-filter.pipe';

describe('CardNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CardNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
